# NCP design skeleton

Static Next.js preview of the Nigerian Community Peterborough site. Three pages, no CMS, no auth. Follows `instruction.md`.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

- `/` Home
- `/about` About
- `/get-involved` Get involved
