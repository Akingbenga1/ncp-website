# Build context for deploy/Dockerfile.next (repo root)
node_modules
.next
.git
.cursor
.vercel
Sprints
Project-documents
# Strapi lives in its own Railway service — keep it out of the Next build
cms
*.log
.env
.env*.local
deploy/.env.prod
tsconfig.tsbuildinfo
